version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - ./dashboard/final-example:/app
      - /app/node_modules
    environment:
      - PORT=3001
      - NODE_ENV=development
    command: pnpm dev


